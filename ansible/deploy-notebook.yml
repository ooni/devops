---
- name: Deploy notebook host
  hosts: notebook1.htz-fsn.prod.ooni.nu
  become: true
  tags:
    - notebook
  vars:
    enable_oonipipeline_worker: false
    ssl_domains:
      - "{{ inventory_hostname }}"
      - "notebook.ooni.org"
  roles:
    - oonidata

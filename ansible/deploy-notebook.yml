---
- name: Deploy notebook host
  hosts: notebook.ooni.org
  become: true
  tags:
    - notebook
  vars:
    notebook_domain: "notebook.ooni.org"
    ssl_domains:
      - "{{ inventory_hostname }}"
      - "notebook.ooni.org"
  roles:
    - notebook

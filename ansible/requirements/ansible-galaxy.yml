roles: 
  - src: nginxinc.nginx
    version: 0.26.0
  - src: geerlingguy.certbot
    version: 5.2.0
  # idealista.clickhouse_role: vendored at ansible/roles/idealista.clickhouse_role
  # (with local fixes) instead of pulled from Galaxy/git at install time.
  # See ansible/roles/idealista.clickhouse_role/VENDORED.md for why and provenance.
  - src: https://github.com/ooni/airflow-role.git
    scm: git
    name: ooni.airflow_role
  - src: geerlingguy.docker # installs docker
    version: 7.4.7
collections: 
  - name: community.docker # manages containers
    version: 4.6.1
